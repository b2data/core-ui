import { ComponentThemeOverride } from "../types";

const dialogActionsTheme: ComponentThemeOverride["MuiDialogActions"] = {
  styleOverrides: {
    root: {
      padding: "10px 20px",
    },
  },
  defaultProps: {},
};

export default dialogActionsTheme;
