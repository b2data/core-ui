import { ComponentThemeOverride } from "../types";

const drawerTheme: ComponentThemeOverride["MuiDrawer"] = {
  styleOverrides: {},
  defaultProps: {
    anchor: "left",
    variant: "temporary"
  }
};

export default drawerTheme;
