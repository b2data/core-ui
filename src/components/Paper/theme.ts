import { ComponentThemeOverride } from "../types";

const paperTheme: ComponentThemeOverride["MuiPaper"] = {
  styleOverrides: {},
  defaultProps: {
    variant: "outlined",
  },
};

export default paperTheme;
