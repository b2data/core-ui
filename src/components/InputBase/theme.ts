import { ComponentThemeOverride } from "../types";

const textFieldTheme: ComponentThemeOverride["MuiInputBase"] = {
  styleOverrides: {},
  defaultProps: {
    size: "small",
    fullWidth: true,
  },
};

export default textFieldTheme;
